# App deploy (app.randevum.tr) — build:app → dist
# Deploy: npx wrangler deploy --config wrangler.app.toml
name = "appointment-fe-app"
compatibility_date = "2025-04-01"

[assets]
directory = "dist"
not_found_handling = "single-page-application"
